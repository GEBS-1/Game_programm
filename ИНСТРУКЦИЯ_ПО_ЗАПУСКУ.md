# 🚀 Инструкция по запуску игры

## ⚡ Быстрый способ (для тестирования интерфейса)

Просто откройте файл `index.html` в браузере:
- Двойной клик по файлу `index.html`
- Или правой кнопкой → "Открыть с помощью" → браузер

**⚠️ Внимание:** Без Firebase игра не будет работать полностью (не будет синхронизации данных).

---

## 🔥 Полная настройка с Firebase (для реальной игры)

### Шаг 1: Создание проекта Firebase (5 минут)

1. Откройте https://console.firebase.google.com/
2. Войдите через Google аккаунт
3. Нажмите **"Создать проект"** или **"Add project"**
4. Введите название проекта (например: `gossimulator`)
5. Нажмите **"Продолжить"**
6. Отключите Google Analytics (или оставьте включенным - не важно)
7. Нажмите **"Создать проект"**
8. Дождитесь создания (10-20 секунд)
9. Нажмите **"Продолжить"**

### Шаг 2: Создание базы данных

1. В боковом меню слева найдите **"Realtime Database"** (База данных в реальном времени)
2. Нажмите **"Создать базу данных"** или **"Create Database"**
3. Выберите регион (например: `us-central1` или ближайший к вам)
4. Нажмите **"Далее"**
5. Выберите **"Начать в тестовом режиме"** (Start in test mode)
6. Нажмите **"Включить"**
7. **Скопируйте URL базы данных** (например: `https://gossimulator-default-rtdb.firebaseio.com/`)

### Шаг 3: Получение конфигурации

1. В Firebase Console нажмите на иконку ⚙️ (шестерёнка) рядом с "Project Overview"
2. Выберите **"Настройки проекта"** или **"Project settings"**
3. Прокрутите вниз до раздела **"Ваши приложения"** или **"Your apps"**
4. Если веб-приложения нет:
   - Нажмите **"Добавить приложение"** → **"Web"** (иконка `</>`)
   - Введите название (например: `ГосСимулятор`)
   - Нажмите **"Зарегистрировать приложение"**
5. Вы увидите конфигурацию Firebase SDK, похожую на это:

```javascript
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "gossimulator.firebaseapp.com",
  databaseURL: "https://gossimulator-default-rtdb.firebaseio.com",
  projectId: "gossimulator",
  storageBucket: "gossimulator.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef123456"
};
```

6. **Скопируйте все эти значения**

### Шаг 4: Настройка файла конфигурации

1. Откройте файл `firebase-config.js` в редакторе
2. Замените значения на скопированные из Firebase:

```javascript
export const firebaseConfig = {
    apiKey: "AIzaSy...",  // ← ваше значение
    authDomain: "gossimulator.firebaseapp.com",  // ← ваше значение
    databaseURL: "https://gossimulator-default-rtdb.firebaseio.com",  // ← ваше значение
    projectId: "gossimulator",  // ← ваше значение
    storageBucket: "gossimulator.appspot.com",  // ← ваше значение
    messagingSenderId: "123456789",  // ← ваше значение
    appId: "1:123456789:web:abcdef123456"  // ← ваше значение
};
```

3. Сохраните файл

### Шаг 5: Настройка правил безопасности

1. В Firebase Console перейдите в **Realtime Database**
2. Откройте вкладку **"Правила"** или **"Rules"**
3. Замените правила на:

```json
{
  "rules": {
    "games": {
      "$gameCode": {
        ".read": true,
        ".write": true
      }
    }
  }
}
```

4. Нажмите **"Опубликовать"** или **"Publish"**

**⚠️ Внимание:** Эти правила открывают доступ для всех. Для продакшена настройте более строгие правила!

### Шаг 6: Запуск игры

1. Откройте `index.html` в браузере
2. В консоли браузера (F12) не должно быть ошибок
3. Попробуйте создать игру!

---

## 🧪 Тестирование

1. Откройте `index.html` в **двух вкладках** браузера
2. В первой вкладке: нажмите **"Создать новую игру"** (ведущий)
3. Скопируйте код игры
4. Во второй вкладке: введите код и имя, нажмите **"Присоединиться"** (игрок)
5. Проверьте, что игрок появился в игре

---

## 🐛 Решение проблем

### Ошибка: "Firebase: Error (auth/configuration-not-found)"
- Проверьте, что файл `firebase-config.js` заполнен правильно
- Убедитесь, что все значения скопированы без ошибок

### Ошибка: "Permission denied"
- Проверьте правила безопасности в Firebase Console
- Убедитесь, что правила опубликованы

### Игра не синхронизируется
- Проверьте консоль браузера (F12) на наличие ошибок
- Убедитесь, что Realtime Database создана и активна
- Проверьте подключение к интернету

### Данные не загружаются
- Проверьте, что файлы в папке `data/` доступны
- При использовании локального файла (file://) используйте локальный сервер

---

## 📦 Запуск через локальный сервер (рекомендуется)

Для корректной работы с JSON файлами лучше использовать локальный сервер:

### Вариант 1: Python
```bash
python -m http.server 8000
```
Затем откройте: http://localhost:8000

### Вариант 2: Node.js
```bash
npx http-server
```
Затем откройте указанный адрес (обычно http://localhost:8080)

### Вариант 3: PHP
```bash
php -S localhost:8000
```
Затем откройте: http://localhost:8000

---

## ✅ Готово!

Теперь игра должна работать полностью! Вы можете:
- Создавать игры
- Присоединяться к играм
- Играть в реальном времени
- Видеть синхронизацию данных

**Удачной игры! 🎮**

