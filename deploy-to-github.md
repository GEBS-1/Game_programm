# 🚀 Размещение на GitHub Pages

## ⚠️ Важно

GitHub Pages поддерживает только **статические файлы** (HTML, CSS, JS). 
Локальный сервер (`server.js`) там работать **не будет**.

## ✅ Решение: Firebase

Для GitHub Pages нужно использовать **Firebase** вместо локального сервера.

---

## 📋 Быстрая инструкция (5 минут)

### Шаг 1: Настроить Firebase

1. Откройте https://console.firebase.google.com/
2. Создайте проект (если ещё не создан)
3. Создайте **Realtime Database** (тестовый режим)
4. Получите конфигурацию (см. `ШАГИ_НАСТРОЙКИ_FIREBASE.md`)
5. Заполните `firebase-config.js` своими данными

### Шаг 2: Переключить на Firebase версию

В файлах `index.html` и `game.html` замените:

**Было:**
```html
<script type="module" src="script-local.js"></script>
```

**Станет:**
```html
<script type="module" src="script.js"></script>
```

### Шаг 3: Создать репозиторий на GitHub

1. Откройте https://github.com/
2. Нажмите **"+"** → **"New repository"**
3. Название: `gossimulator` (или любое другое)
4. Выберите **Public**
5. Нажмите **"Create repository"**

### Шаг 4: Загрузить файлы

**НЕ загружайте:**
- ❌ `node_modules/` (папка)
- ❌ `games/` (папка с данными игр)
- ❌ `server.js` (не нужен)
- ❌ `package.json` (не нужен)
- ❌ `script-local.js` (не нужен)

**Загрузите:**
- ✅ `index.html`
- ✅ `game.html`
- ✅ `style.css`
- ✅ `script.js` (версия для Firebase)
- ✅ `firebase-config.js` (⚠️ будет виден всем)
- ✅ `data/` (все JSON файлы)
- ✅ `README.md`

**Команды Git:**
```bash
git init
git add index.html game.html style.css script.js firebase-config.js data/ README.md
git commit -m "Initial commit: ГосСимулятор для GitHub Pages"
git branch -M main
git remote add origin https://github.com/ВАШ_ЛОГИН/gossimulator.git
git push -u origin main
```

### Шаг 5: Настроить GitHub Pages

1. В репозитории: **Settings** → **Pages**
2. Source: **Deploy from a branch**
3. Branch: **main**
4. Folder: **/ (root)**
5. Нажмите **Save**

### Шаг 6: Готово! 🎉

Через 1-2 минуты игра будет доступна:
```
https://ВАШ_ЛОГИН.github.io/gossimulator/
```

---

## 🔄 Автоматическое переключение

Могу создать версию, которая автоматически определяет:
- Если Firebase настроен → использует Firebase
- Если нет → показывает сообщение

Это позволит использовать один код для локальной разработки и GitHub Pages.

---

## ⚠️ Безопасность

`firebase-config.js` будет виден всем в публичном репозитории. Это нормально для Realtime Database в тестовом режиме.

Для продакшена настройте правила безопасности в Firebase Console.

---

**Нужна помощь с настройкой Firebase? Скажите!**

