# 🚀 Загрузка на GitHub Pages - Пошаговая инструкция

## ✅ Что уже готово:
- ✅ Firebase настроен
- ✅ Конфигурация заполнена
- ✅ HTML файлы переключены на версию с Firebase

---

## 📋 Шаг 1: Создать репозиторий на GitHub

1. Откройте https://github.com/
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите **"+"** в правом верхнем углу → **"New repository"**
4. Заполните:
   - **Repository name:** `gossimulator` (или любое другое имя)
   - **Description:** "ГосСимулятор: Карьерный старт - веб-версия игры"
   - Выберите **Public** (публичный)
   - **НЕ** ставьте галочки на "Initialize this repository with..."
5. Нажмите **"Create repository"**

---

## 📋 Шаг 2: Загрузить файлы через Git

Откройте терминал/командную строку в папке проекта и выполните:

### 2.1. Инициализация Git
```bash
git init
```

### 2.2. Добавление файлов
```bash
git add index.html game.html style.css script.js firebase-config.js data/ README.md
```

**Или добавить все нужные файлы:**
```bash
git add index.html game.html style.css script.js firebase-config.js
git add data/
git add README.md
```

### 2.3. Первый коммит
```bash
git commit -m "Initial commit: ГосСимулятор для GitHub Pages"
```

### 2.4. Подключение к удалённому репозиторию
**ЗАМЕНИТЕ `ВАШ_ЛОГИН` на ваш реальный логин GitHub!**

```bash
git branch -M main
git remote add origin https://github.com/ВАШ_ЛОГИН/gossimulator.git
```

### 2.5. Отправка на GitHub
```bash
git push -u origin main
```

Если попросит авторизацию:
- Используйте Personal Access Token вместо пароля
- Или используйте GitHub Desktop (проще)

---

## 📋 Шаг 3: Настроить GitHub Pages

1. На GitHub откройте ваш репозиторий
2. Перейдите в **Settings** (вверху справа в репозитории)
3. В боковом меню найдите **Pages**
4. В разделе **Source**:
   - Выберите **Branch: main**
   - Выберите **Folder: / (root)**
5. Нажмите **Save**

---

## 📋 Шаг 4: Готово! 🎉

Через 1-2 минуты игра будет доступна по адресу:

```
https://ВАШ_ЛОГИН.github.io/gossimulator/
```

**Пример:** Если ваш логин `ivan123`, то адрес будет:
```
https://ivan123.github.io/gossimulator/
```

---

## ⚠️ Важно: Какие файлы НЕ загружать

**НЕ загружайте на GitHub:**
- ❌ `node_modules/` (папка)
- ❌ `games/` (папка с данными игр - локальные данные)
- ❌ `server.js` (не нужен для GitHub Pages)
- ❌ `package.json` (не нужен для GitHub Pages)
- ❌ `script-local.js` (не нужен для GitHub Pages)
- ❌ `start-server.bat` (не нужен)

**Загрузите:**
- ✅ `index.html`
- ✅ `game.html`
- ✅ `style.css`
- ✅ `script.js` (версия для Firebase)
- ✅ `firebase-config.js` (⚠️ будет виден всем, но это нормально)
- ✅ `data/` (все JSON файлы)
- ✅ `README.md` (опционально)

---

## 🔄 Обновление игры

После изменений в коде:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Изменения появятся на GitHub Pages через 1-2 минуты.

---

## 🐛 Решение проблем

### Ошибка: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/ВАШ_ЛОГИН/gossimulator.git
```

### Ошибка авторизации
- Используйте Personal Access Token вместо пароля
- Или используйте GitHub Desktop (проще)

### Игра не работает на GitHub Pages
- Проверьте консоль браузера (F12) на ошибки
- Убедитесь, что правила безопасности в Firebase настроены
- Проверьте, что `firebase-config.js` загружен на GitHub

---

**Удачи! 🎮**

