# 🚀 Деплой на GitHub Pages

## ⚠️ Важно: GitHub Pages не поддерживает Node.js серверы!

GitHub Pages может размещать только **статические файлы** (HTML, CSS, JS). Наш локальный сервер (`server.js`) там работать не будет.

## ✅ Решение: Использовать Firebase

Для работы на GitHub Pages нужно использовать Firebase вместо локального сервера.

---

## 📋 Пошаговая инструкция

### Шаг 1: Настроить Firebase

1. Откройте https://console.firebase.google.com/
2. Создайте проект (если ещё не создан)
3. Создайте Realtime Database (тестовый режим)
4. Получите конфигурацию (см. `ШАГИ_НАСТРОЙКИ_FIREBASE.md`)
5. Заполните `firebase-config.js`

### Шаг 2: Переключить на Firebase версию

В файлах `index.html` и `game.html` замените:

```html
<!-- Было: -->
<script type="module" src="script-local.js"></script>

<!-- Станет: -->
<script type="module" src="script.js"></script>
```

### Шаг 3: Создать репозиторий на GitHub

1. Откройте https://github.com/
2. Создайте новый репозиторий (например: `gossimulator`)
3. Выберите **Public**

### Шаг 4: Загрузить файлы

**ВАЖНО:** Не загружайте на GitHub:
- `node_modules/` (папка)
- `games/` (папка с данными игр)
- `server.js` (не нужен для GitHub Pages)
- `package.json` (не нужен для GitHub Pages)
- `firebase-config.js` (содержит секретные данные!)

**Загрузите:**
- `index.html`
- `game.html`
- `style.css`
- `script.js` (версия для Firebase)
- `firebase-config.js` (⚠️ будет виден всем, но для тестового режима это нормально)
- `data/` (все JSON файлы)
- `README.md`

### Шаг 5: Настроить GitHub Pages

1. В репозитории: **Settings** → **Pages**
2. Source: **Deploy from a branch**
3. Branch: **main** (или **master**)
4. Folder: **/ (root)**
5. Нажмите **Save**

### Шаг 6: Готово!

Через 1-2 минуты игра будет доступна по адресу:
```
https://ваш-логин.github.io/gossimulator/
```

---

## 🔄 Альтернатива: Два варианта в одном проекте

Можно сделать так, чтобы игра автоматически определяла, использовать Firebase или локальный сервер:

1. Если Firebase настроен → использует Firebase
2. Если нет → показывает сообщение о необходимости настройки

Это позволит использовать один код для локальной разработки и GitHub Pages.

---

## 📝 Что включить в .gitignore

```
node_modules/
games/
*.log
.DS_Store
```

---

## ⚠️ Безопасность

`firebase-config.js` будет виден всем в публичном репозитории. Это нормально для Realtime Database в тестовом режиме, но:

- Для продакшена настройте правила безопасности в Firebase
- Или используйте Environment Variables (более сложно)

---

**Нужна помощь с настройкой? Скажите, и я помогу!**

